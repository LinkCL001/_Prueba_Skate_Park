<form method="POST" action=`/skater-delete/{{skaters.id}}` enctype="multipart/form-data">
  {{#each skaters}}
    <div class="container p-5 text-center">
      <h1>¿Estas seguro de eliminar el usuario
        <strong>{{skaters.nombre}}</strong>?</h1>
      <a
        href="/skater-delete/{{skaters.id}}"
        class="btn btn-danger"
      >Confirmar</a>
      <a href="/" class="btn btn-secondary">Cancelar</a>
    </div>
  {{/each}}
</form>